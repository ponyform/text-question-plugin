{% if question.extra.multiline %}
    <textarea {{ inputAttributes|raw }} class="ponyform__textquestionplugin__textarea" rows="5">longer</textarea>
{% else %}
    <input {{ inputAttributes|raw }} type="text" class="ponyform__textquestionplugin__input" value="shorty" />
{% endif %}